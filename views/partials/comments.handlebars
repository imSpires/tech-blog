<div class="comments">
  {{#each this}}
    <div class="">
        <h3 class=""></h3>
        <section class="">
            <div class="">
                {{comment_text}}
            </div>
            <div class="meta">
                -{{user.username}}, {{format_date created_at}}
            </div>
        </section>
    </div>
  {{/each}}
</div>